@inherits Umbraco.Web.Mvc.UmbracoViewPage
@{
    Layout = "layout.cshtml";
}
<div class="container-fluid">
    <div class="row">
        <div class="col-md-3">
            @Html.Partial("Eshop/_ProtectNavigation")
        </div>
        <div class="col-md-9">
            @Html.GetGridHtml(Model, "gridContent", "Bootstrap3-Fluid")
        </div>
    </div>
</div>